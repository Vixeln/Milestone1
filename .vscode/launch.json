{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Aaron Release",
            "type": "cppdbg",
            "request": "launch",
            "preLaunchTask": "Compile Release Sequence", // this is referencing to the tasks.json name "
            "cwd": "${cwd}",
            "program": "${cwd}/build/Test", // The 'Test' program/executable name comes from the CMakeLists.txt
            "MIMode": "lldb"
        },
    ],
}